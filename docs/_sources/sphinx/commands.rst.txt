========
Commands
========


Common commands that I use with sphinx to write this documentation.

Create the directory structure for the documentation
and all the needed build files:

.. code-block:: bash

    $ sphinx-quickstart

Build a html documentation:

.. code-block:: bash

    $ make html

Clean up the build folder:

.. code-block:: bash

    $ make clean
